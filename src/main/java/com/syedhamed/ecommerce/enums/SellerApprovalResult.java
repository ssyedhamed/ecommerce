package com.syedhamed.ecommerce.enums;

public enum SellerApprovalResult {
    APPROVED,
    ALREADY_APPROVED,
    ALREADY_REJECTED,
    NOT_REQUESTED,
    ALREADY_PENDING,
    REJECTED,
    PENDING
}
